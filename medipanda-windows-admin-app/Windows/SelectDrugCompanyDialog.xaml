<Window x:Class="medipanda_windows_admin.Windows.SelectDrugCompanyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="정산파일 변환" 
        Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 정산월 선택 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="정산월 선택" 
                       FontSize="14" 
                       FontWeight="Bold"
                       Margin="0,0,0,8"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="YearComboBox" 
                          Grid.Column="0"
                          Height="32"
                          FontSize="13"
                          SelectionChanged="YearComboBox_SelectionChanged"/>
                <ComboBox x:Name="MonthComboBox" 
                          Grid.Column="2"
                          Height="32"
                          FontSize="13"/>
            </Grid>
        </StackPanel>

        <!-- 제약사 선택 헤더 -->
        <TextBlock Grid.Row="1" 
                   Text="제약사 선택" 
                   FontSize="14" 
                   FontWeight="Bold"
                   Margin="0,0,0,8"/>

        <!-- 제약사 리스트 -->
        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="1">
            <Grid>
                <ListBox x:Name="DrugCompanyListBox"
                         BorderThickness="0"
                         SelectionMode="Single">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" 
                                       Padding="8,5"
                                       FontSize="13"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- 로딩 표시 -->
                <TextBlock x:Name="LoadingText"
                           Text="불러오는 중..."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="#999"
                           Visibility="Visible"/>
            </Grid>
        </Border>

        <!-- 버튼 영역 -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="취소" 
                    Width="80" Height="32"
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>
            <Button x:Name="ConfirmButton"
                    Content="확인" 
                    Width="80" Height="32"
                    Background="#0E2148"
                    Foreground="White"
                    BorderThickness="0"
                    IsEnabled="False"
                    Click="Confirm_Click"/>
        </StackPanel>
    </Grid>
</Window>